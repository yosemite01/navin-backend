openapi: 3.0.0
info:
  title: Navin Backend API
  version: 1.0.0
paths:
  /api/webhooks/iot:
    post:
      summary: IoT webhook endpoint for telemetry data
      description: Receives telemetry data from IoT sensors. Requires API key authentication.
      security:
        - apiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - shipmentId
                - temperature
                - humidity
                - latitude
                - longitude
                - batteryLevel
                - timestamp
              properties:
                shipmentId:
                  type: string
                  description: MongoDB ObjectId of the shipment
                temperature:
                  type: number
                  description: Temperature in Celsius
                humidity:
                  type: number
                  description: Humidity percentage
                latitude:
                  type: number
                  description: GPS latitude
                longitude:
                  type: number
                  description: GPS longitude
                batteryLevel:
                  type: number
                  description: Battery level percentage
                timestamp:
                  type: string
                  format: date-time
                  description: Timestamp of the telemetry reading
      responses:
        '201':
          description: Telemetry data received and processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      _id:
                        type: string
                      shipmentId:
                        type: string
                      temperature:
                        type: number
                      humidity:
                        type: number
                      latitude:
                        type: number
                      longitude:
                        type: number
                      batteryLevel:
                        type: number
                      timestamp:
                        type: string
                        format: date-time
                      dataHash:
                        type: string
                      stellarTxHash:
                        type: string
        '400':
          description: Invalid request payload
        '401':
          description: Missing or invalid API key
  /api/auth/api-keys:
    post:
      summary: Create a new API key
      description: Generates a new API key for machine-to-machine authentication. The raw API key is returned only once.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - organizationId
              properties:
                name:
                  type: string
                  description: Descriptive name for the API key
                organizationId:
                  type: string
                  description: MongoDB ObjectId of the organization
                shipmentId:
                  type: string
                  description: Optional MongoDB ObjectId of a specific shipment
      responses:
        '201':
          description: API key created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      apiKey:
                        type: string
                        description: Raw API key (shown only once)
                      id:
                        type: string
                      name:
                        type: string
                      organizationId:
                        type: string
                      shipmentId:
                        type: string
                      createdAt:
                        type: string
                        format: date-time
        '400':
          description: Invalid request
        '401':
          description: Unauthorized
  /api/auth/login:
    post:
      summary: Authenticate a user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  data:
                    type: object
                  meta:
                    type: object
        '401':
          description: Invalid credentials
        '429':
          description: Too many login attempts
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  retryAfter:
                    type: integer
                  data:
                    type: 'null'
  /api/auth/api-keys/{organizationId}:
    get:
      summary: List API keys for an organization
      description: Returns all active API keys for the specified organization (without exposing the raw keys)
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organizationId
          required: true
          schema:
            type: string
          description: MongoDB ObjectId of the organization
      responses:
        '200':
          description: List of API keys
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          type: string
                        name:
                          type: string
                        organizationId:
                          type: string
                        shipmentId:
                          type: string
                        isActive:
                          type: boolean
                        lastUsedAt:
                          type: string
                          format: date-time
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Unauthorized
  /api/auth/api-keys/{apiKeyId}:
    delete:
      summary: Revoke an API key
      description: Deactivates an API key, preventing it from being used for authentication
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: apiKeyId
          required: true
          schema:
            type: string
          description: MongoDB ObjectId of the API key
      responses:
        '200':
          description: API key revoked successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '401':
          description: Unauthorized
        '404':
          description: API key not found
  /api/users/invitations:
    post:
      summary: Generate user invitation link
      description: Creates a signed 48-hour invitation token bound to the inviter organization and target role.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - role
              properties:
                email:
                  type: string
                  format: email
                role:
                  type: string
                  enum: [ADMIN, MANAGER, VIEWER, CUSTOMER]
      responses:
        '201':
          description: Invitation generated
        '403':
          description: Forbidden
  /api/users/invitations/verify:
    get:
      summary: Verify invitation token
      parameters:
        - in: query
          name: token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Invitation token is valid
        '401':
          description: Invalid or expired invitation token
  /api/users/invitations/accept:
    post:
      summary: Accept invitation and create account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
                - name
                - password
              properties:
                token:
                  type: string
                name:
                  type: string
                password:
                  type: string
                  minLength: 6
      responses:
        '201':
          description: Invitation accepted
        '401':
          description: Invalid or expired invitation token
        '409':
          description: Email already in use
  /api/analytics/performance:
    get:
      summary: Get analytics performance dashboard
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: startDate
          required: true
          schema:
            type: string
            format: date-time
        - in: query
          name: endDate
          required: true
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Performance dashboard
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      startDate:
                        type: string
                        format: date-time
                      endDate:
                        type: string
                        format: date-time
                      shipmentsByStatus:
                        type: array
                        items:
                          type: object
                          properties:
                            status:
                              type: string
                            total:
                              type: integer
                      averageDeliveryTimeByLogisticsId:
                        type: array
                        items:
                          type: object
                          properties:
                            logisticsId:
                              type: string
                            averageDeliveryTimeMs:
                              type: number
                      totalDelayedShipments:
                        type: integer
  /api/shipments:
    get:
      summary: Get paginated shipments
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [CREATED, IN_TRANSIT, DELIVERED, CANCELLED]
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: limit
          schema:
            type: integer
      responses:
        '200':
          description: Paginated shipments
    post:
      summary: Create a shipment
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Shipment'
      responses:
        '201':
          description: Shipment created
  /api/shipments/{id}:
    patch:
      summary: Update off-chain metadata
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                offChainMetadata:
                  type: object
      responses:
        '200':
          description: Shipment updated
  /api/shipments/{id}/proof:
    post:
      summary: Upload proof of delivery for a shipment
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                recipientSignatureName:
                  type: string
                notes:
                  type: string
      responses:
        '200':
          description: Proof uploaded successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      deliveryProof:
                        type: object
                        properties:
                          url:
                            type: string
                          recipientSignatureName:
                            type: string
                          notes:
                            type: string
                          uploadedAt:
                            type: string
                            format: date-time
        '400':
          description: Missing file or invalid payload
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '503':
          description: Storage unavailable
  /api/anomalies:
    get:
      summary: Get anomalies with pagination and filtering
      description: Retrieve a paginated list of anomalies, sorted by timestamp descending. Supports filtering by severity and shipmentId. Requires ADMIN or MANAGER role.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
          description: Cursor for pagination
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
          description: Number of items per page
        - in: query
          name: shipmentId
          schema:
            type: string
          description: Filter by shipment ID
        - in: query
          name: severity
          schema:
            type: string
            enum: [LOW, MEDIUM, HIGH]
          description: Filter by severity
      responses:
        '200':
          description: Paginated anomalies
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Anomaly'
                  nextCursor:
                    type: string
                    nullable: true
                  hasMore:
                    type: boolean
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - insufficient role
  /api/anomalies/{id}/resolve:
    patch:
      summary: Resolve an anomaly
      description: Mark an anomaly as resolved. Requires ADMIN or MANAGER role.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Anomaly ID
      responses:
        '200':
          description: Anomaly resolved
          content:
            application/json:
              schema:
                type: object
                properties:
                  anomaly:
                    $ref: '#/components/schemas/Anomaly'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - insufficient role
        '404':
          description: Anomaly not found
components:
  schemas:
    Shipment:
      type: object
      properties:
        trackingNumber:
          type: string
        origin:
          type: string
        destination:
          type: string
        enterpriseId:
          type: string
        logisticsId:
          type: string
        status:
          type: string
          enum: [CREATED, IN_TRANSIT, DELIVERED, CANCELLED]
        milestones:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              timestamp:
                type: string
                format: date-time
              description:
                type: string
        offChainMetadata:
          type: object
    Anomaly:
      type: object
      properties:
        _id:
          type: string
        shipmentId:
          type: string
        type:
          type: string
          enum: [TEMPERATURE_EXCEEDED, TEMPERATURE_BELOW_MIN, HUMIDITY_EXCEEDED, HUMIDITY_BELOW_MIN, BATTERY_LOW]
        severity:
          type: string
          enum: [LOW, MEDIUM, HIGH]
        message:
          type: string
        timestamp:
          type: string
          format: date-time
        resolved:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
securitySchemes:
  bearerAuth:
    type: http
    scheme: bearer
  apiKeyAuth:
    type: apiKey
    in: header
    name: x-api-key
    description: API key for IoT device authentication
